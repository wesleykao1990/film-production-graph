# M09 — Automated Provider Adapters

Implement common protocols and selected automated adapters for actual needs: capabilities, policy snapshots, submit/poll/webhook/cancel, normalized errors/results, costs, idempotency, retries, budgets, callbacks, and provider-specific compilers.

Keep the manual adapter. Same Shot Contract must work through two adapters without changing canon/audio.

Exit: compare two provider takes with complete provenance and no core-domain changes.
