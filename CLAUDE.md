# Claude Code Instructions

Read and follow `AGENTS.md`, then `START_HERE.md`, `PRD.md`, `IMPLEMENTATION_PLAN.md`, `prototype/README.md`, and the current milestone prompt.

Implement one milestone at a time. Run the package validator and keep the prototype tests green, but do not port the prototype's SQLite/static/mock implementation into production. Before M04a PASS and M04b completion, do not add a durable workflow engine, hosted skill registry, or model-gateway service. Do not execute uploaded skill scripts. Do not make live provider calls in ordinary tests. Preserve canon, impact, rights, audio, delivery, authority, and lineage invariants.
